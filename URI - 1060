#include <stdio.h>
 
int main() {
    double a;
    int i;
    int positivo = 0;
    for (i = 0; i < 6; i++){
        scanf("%lf", &a);
        if (a > 0){
            positivo++;
        }
    }
    printf("%d valores positivos\n", positivo);
    return 0;
}
