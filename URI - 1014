#include <stdio.h>
int main() {
    int km;
    double combustivel;
    double consumo;
        scanf("%d",&km);
        scanf("%lf",&combustivel);
        consumo=km/combustivel;
            printf("%.3lf km/l\n",consumo);
    return 0;
}
