#include <stdio.h>
int main() {
    int A;
    int B;
    int C;
    int D;
        scanf("%d", &A);
        scanf("%d", &B);
        scanf("%d", &C);
        scanf("%d", &D);
            printf("DIFERENCA = %d\n", A*B-C*D);
    return 0;
}
