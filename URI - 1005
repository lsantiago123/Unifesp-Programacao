#include <stdio.h>
int main() {
    double nota1;
    double nota2;
    double resultado;
        scanf("%lf", &nota1);
        scanf("%lf", &nota2);
        resultado = (nota1 * 3.5 + nota2 * 7.5) / 11;
            printf("MEDIA = %.5lf\n", resultado);
    return 0;
}
